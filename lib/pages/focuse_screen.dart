import 'package:flutter/material.dart';

class FocuseScreen extends StatelessWidget {
  const FocuseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Focuse Screen'),
    );
  }
}